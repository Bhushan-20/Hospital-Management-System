spring.application.name=UserMS

# MySQL DB connection
spring.datasource.url=jdbc:mysql://localhost:3306/user_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Bhushan@0720
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
#This will create the table in DB automatically based on the Model
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Port
server.port=4000

#Exceptions
USER_ALREADY_EXIST = User Already Exist.
USER_NOT_FOUND = User Not Found.
INVALID_CREDENTIALS= Invalid Password or Email.
